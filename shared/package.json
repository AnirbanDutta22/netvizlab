{
  "name": "@netviz/shared",
  "private": true,
  "version": "1.0.0",
  "description": "Types, DTOs, error contracts, and constants shared between the Network Visualizer client and server. No build step — consumed directly as TypeScript source by both Vite (client) and tsx (server).",
  "type": "module",
  "main": "src/index.ts",
  "types": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  }
}
