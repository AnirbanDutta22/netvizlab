# Network Visualizer

An interactive tool for running networking commands (Ping,
Traceroute, DNS Lookup) and watching the underlying concepts animate in
real time, instead of just reading raw terminal output.
